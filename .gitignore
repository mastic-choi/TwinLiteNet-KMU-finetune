# 데이터셋 & 파생 데이터 (용량 크고, 재생성 가능한 것들 — 별도로 zip/Drive로 옮길 것)
dataset/
dataset_diet/
dataset_diet_v2/
dataset_overlay/
dataset_balanced/
raw/
raw 2/
bootstrap/
bootstrap_refined/
bootstrap_v2/
pseudo_dataset/
pseudo_dataset_v2/
labeling_handoff/
labeling_request_hard_frames/
missing_line_frames/
ensemble_drafts/
diet_montages/
new_raw_frames/
trust_review_output/
.gradio/
_handoff_extracted/
_cvat_export_extracted/
archive/

# 외부에서 클론한 레포 (각자 자체 .git 있음 — nested repo 문제 방지)
YOLOPv2/
_TwinLiteNetPlus_ref/
Ultra-Fast-Lane-Detection-v2/
CLRerNet/
PINTO_CLRerNet/
CLRNet/
GANet/
CondLaneNet/
PETR/
LaneGAP/

# 생성된 결과물 - 몽타주/스크래치 이미지는 결과물이라 추적함(outputs/montages, outputs/scratch).
# onnx 가중치만 제외(위 *.onnx 규칙과 중복이지만 명시적으로 한 번 더).
outputs/onnx/

# 대용량 바이너리 (모델 가중치, 압축 데이터셋)
*.zip
*.onnx
*.onnx.data
*.pth
*.pt

# 예외: 최종 배포용 onnx는 추적 (실차 반영 대상, README에서 링크)
!outputs/models/best.onnx
!outputs/models/best.onnx.data

# 데이터셋에서 파생된 CSV/JSON (재생성 가능)
*.csv
coco_import_rle.json

# 파이썬 가상환경 / 캐시
.venv_triage/
.venv_yolo/
__pycache__/
*.pyc
.DS_Store
.ipynb_checkpoints/
